/**
 * Shell Data & Reporting APIsLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { array, lazy, object, optional, Schema, string } from '../schema';
import {
  BonusConfiguration,
  bonusConfigurationSchema,
} from './bonusConfiguration';
import { ErrorStatus, errorStatusSchema } from './errorStatus';
import {
  PricingCurrentVolume,
  pricingCurrentVolumeSchema,
} from './pricingCurrentVolume';
import { PricingHistory, pricingHistorySchema } from './pricingHistory';

export interface VolumeBasedPricingResponse {
  configuration?: BonusConfiguration[];
  currentPeriodConsumption?: PricingCurrentVolume[];
  history?: PricingHistory[];
  error?: ErrorStatus;
  /** API Request Id */
  requestId?: string;
}

export const volumeBasedPricingResponseSchema: Schema<VolumeBasedPricingResponse> = object(
  {
    configuration: [
      'Configuration',
      optional(array(lazy(() => bonusConfigurationSchema))),
    ],
    currentPeriodConsumption: [
      'CurrentPeriodConsumption',
      optional(array(lazy(() => pricingCurrentVolumeSchema))),
    ],
    history: ['History', optional(array(lazy(() => pricingHistorySchema)))],
    error: ['Error', optional(lazy(() => errorStatusSchema))],
    requestId: ['RequestId', optional(string())],
  }
);
