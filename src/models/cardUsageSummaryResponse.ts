/**
 * Shell Data & Reporting APIsLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { array, lazy, object, optional, Schema, string } from '../schema';
import { ErrorStatus, errorStatusSchema } from './errorStatus';
import { UsageSummary, usageSummarySchema } from './usageSummary';

export interface CardUsageSummaryResponse {
  usageSummary?: UsageSummary[];
  /** API Request Id */
  requestId?: string;
  error?: ErrorStatus;
}

export const cardUsageSummaryResponseSchema: Schema<CardUsageSummaryResponse> = object(
  {
    usageSummary: [
      'UsageSummary',
      optional(array(lazy(() => usageSummarySchema))),
    ],
    requestId: ['RequestId', optional(string())],
    error: ['Error', optional(lazy(() => errorStatusSchema))],
  }
);
